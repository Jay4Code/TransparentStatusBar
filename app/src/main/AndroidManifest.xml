<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lga.transparentstatusbar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Splash1Activity" />
        <activity android:name=".Splash2Activity" />
        <activity
            android:name=".Image1Activity"
            android:theme="@style/ImageTranslucentBarTheme" />
        <activity
            android:name=".Image2Activity"
            android:theme="@style/ImageTranslucentBarTheme.NoActionBar" />
        <activity
            android:name=".Color1Activity"
            android:theme="@style/ColorTranslucentBarTheme" />
        <activity
            android:name=".Color2Activity"
            android:theme="@style/ColorTranslucentBarTheme.NoActionBar" />
    </application>

</manifest>